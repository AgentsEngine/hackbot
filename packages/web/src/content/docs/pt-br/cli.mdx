---
title: CLI
description: Opções e comandos da CLI do hackbot.
---

import { Tabs, TabItem } from "@astrojs/starlight/components"

A CLI do hackbot, por padrão, inicia o [TUI](/docs/tui) quando executada sem argumentos.

```bash
hackbot
```

Mas também aceita comandos conforme documentado nesta página. Isso permite que você interaja com o hackbot programaticamente.

```bash
hackbot run "Explain how closures work in JavaScript"
```

---

### tui

Inicie a interface de usuário do terminal do hackbot.

```bash
hackbot [project]
```

#### Opções

| Flag                                     | Curto | Descrição                                                                  |
| ---------------------------------------- | ----- | -------------------------------------------------------------------------- |
| <nobr><code>{"--continue"}</code></nobr> | `-c`  | Continue a última sessão                                                   |
| <nobr><code>{"--session"}</code></nobr>  | `-s`  | ID da sessão para continuar                                                |
| <nobr><code>{"--fork"}</code></nobr>     |       | Criar um fork da sessão ao continuar (use com `--continue` ou `--session`) |
| <nobr><code>{"--prompt"}</code></nobr>   |       | Prompt a ser usado                                                         |
| <nobr><code>{"--model"}</code></nobr>    | `-m`  | Modelo a ser usado na forma de provider/model                              |
| <nobr><code>{"--agent"}</code></nobr>    |       | Agente a ser usado                                                         |
| <nobr><code>{"--port"}</code></nobr>     |       | Porta para escutar                                                         |
| <nobr><code>{"--hostname"}</code></nobr> |       | Nome do host para escutar                                                  |

---

## Comandos

A CLI do hackbot também possui os seguintes comandos.

---

### agent

Gerencie agentes para o hackbot.

```bash
hackbot agent [command]
```

---

### attach

Anexe um terminal a um servidor backend do hackbot já em execução, iniciado via comandos `serve` ou `web`.

```bash
hackbot attach [url]
```

Isso permite usar o TUI com um backend hackbot remoto. Por exemplo:

```bash
# Inicie o servidor backend para acesso web/mobile
hackbot web --port 4096 --hostname 0.0.0.0

# Em outro terminal, anexe o TUI ao backend em execução
hackbot attach http://10.20.30.40:4096
```

#### Opções

| Flag                                     | Curto | Descrição                                                                       |
| ---------------------------------------- | ----- | ------------------------------------------------------------------------------- |
| <nobr><code>{"--dir"}</code></nobr>      |       | Diretório de trabalho para iniciar o TUI                                        |
| <nobr><code>{"--continue"}</code></nobr> | `-c`  | Continuar a última sessão                                                       |
| <nobr><code>{"--session"}</code></nobr>  | `-s`  | ID da sessão para continuar                                                     |
| <nobr><code>{"--fork"}</code></nobr>     |       | Bifurcar a sessão ao continuar (use com `--continue` ou `--session`)            |
| <nobr><code>{"--password"}</code></nobr> | `-p`  | Senha de autenticação básica (padrão: `HACKBOT_SERVER_PASSWORD`)                |
| <nobr><code>{"--username"}</code></nobr> | `-u`  | Usuário de autenticação básica (padrão: `HACKBOT_SERVER_USERNAME` ou `hackbot`) |

---

#### create

Crie um novo agente com configuração personalizada.

```bash
hackbot agent create
```

Este comando irá guiá-lo na criação de um novo agente com um prompt de sistema personalizado e configuração de ferramentas.

---

#### list

Liste todos os agentes disponíveis.

```bash
hackbot agent list
```

---

### auth

Comando para gerenciar credenciais e login para provedores.

```bash
hackbot auth [command]
```

---

#### login

O hackbot é alimentado pela lista de provedores em [Models.dev](https://models.dev), então você pode usar `hackbot auth login` para configurar chaves de API para qualquer provedor que você gostaria de usar. Isso é armazenado em `~/.local/share/hackbot/auth.json`.

```bash
hackbot auth login
```

Quando o hackbot é iniciado, ele carrega os provedores do arquivo de credenciais. E se houver chaves definidas em seus ambientes ou em um arquivo `.env` em seu projeto.

---

#### list

Lista todos os provedores autenticados conforme armazenado no arquivo de credenciais.

```bash
hackbot auth list
```

Ou a versão curta.

```bash
hackbot auth ls
```

---

#### logout

Desconecta você de um provedor limpando-o do arquivo de credenciais.

```bash
hackbot auth logout
```

---

### github

Gerencie o agente do GitHub para automação de repositórios.

```bash
hackbot github [command]
```

---

#### install

Instale o agente do GitHub em seu repositório.

```bash
hackbot github install
```

Isso configura o fluxo de trabalho necessário do GitHub Actions e o guia pelo processo de configuração. [Saiba mais](/docs/github).

---

#### run

Execute o agente do GitHub. Isso é tipicamente usado em GitHub Actions.

```bash
hackbot github run
```

##### Opções

| Flag                                  | Descrição                                        |
| ------------------------------------- | ------------------------------------------------ |
| <nobr><code>{"--event"}</code></nobr> | Evento simulado do GitHub para executar o agente |
| <nobr><code>{"--token"}</code></nobr> | Token de acesso pessoal do GitHub                |

---

### mcp

Gerencie servidores do Model Context Protocol.

```bash
hackbot mcp [command]
```

---

#### add

Adicione um servidor MCP à sua configuração.

```bash
hackbot mcp add
```

Este comando irá guiá-lo na adição de um servidor MCP local ou remoto.

---

#### list

Liste todos os servidores MCP configurados e seu status de conexão.

```bash
hackbot mcp list
```

Ou use a versão curta.

```bash
hackbot mcp ls
```

---

#### auth

Autentique-se com um servidor MCP habilitado para OAuth.

```bash
hackbot mcp auth [name]
```

Se você não fornecer um nome de servidor, será solicitado que você selecione entre os servidores disponíveis habilitados para OAuth.

Você também pode listar servidores habilitados para OAuth e seu status de autenticação.

```bash
hackbot mcp auth list
```

Ou use a versão curta.

```bash
hackbot mcp auth ls
```

---

#### logout

Remova credenciais OAuth para um servidor MCP.

```bash
hackbot mcp logout [name]
```

---

#### debug

Depure problemas de conexão OAuth para um servidor MCP.

```bash
hackbot mcp debug <name>
```

---

### models

Liste todos os modelos disponíveis dos provedores configurados.

```bash
hackbot models [provider]
```

Este comando exibe todos os modelos disponíveis entre seus provedores configurados no formato `provider/model`.

Isso é útil para descobrir o nome exato do modelo a ser usado em [sua configuração](/docs/config/).

Você pode opcionalmente passar um ID de provedor para filtrar modelos por esse provedor.

```bash
hackbot models anthropic
```

#### Opções

| Flag                                    | Descrição                                                             |
| --------------------------------------- | --------------------------------------------------------------------- |
| <nobr><code>{"--refresh"}</code></nobr> | Atualiza o cache de modelos a partir do models.dev                    |
| <nobr><code>{"--verbose"}</code></nobr> | Use uma saída de modelo mais detalhada (inclui metadados como custos) |

Use a flag `--refresh` para atualizar a lista de modelos em cache. Isso é útil quando novos modelos foram adicionados a um provedor e você deseja vê-los no hackbot.

```bash
hackbot models --refresh
```

---

### run

Execute o hackbot em modo não interativo passando um prompt diretamente.

```bash
hackbot run [message..]
```

Isso é útil para scripts, automação ou quando você deseja uma resposta rápida sem iniciar o TUI completo. Por exemplo.

```bash "hackbot run"
hackbot run Explique o uso de context em Go
```

Você também pode se anexar a uma instância em execução do `hackbot serve` para evitar tempos de inicialização a frio do servidor MCP em cada execução:

```bash
# Inicie um servidor sem cabeça em um terminal
hackbot serve

# Em outro terminal, execute comandos que se anexam a ele
hackbot run --attach http://localhost:4096 "Explique async/await em JavaScript"
```

#### Opções

| Flag                                     | Curto | Descrição                                                                       |
| ---------------------------------------- | ----- | ------------------------------------------------------------------------------- |
| <nobr><code>{"--command"}</code></nobr>  |       | O comando a ser executado, use mensagem para argumentos                         |
| <nobr><code>{"--continue"}</code></nobr> | `-c`  | Continue a última sessão                                                        |
| <nobr><code>{"--session"}</code></nobr>  | `-s`  | ID da sessão para continuar                                                     |
| <nobr><code>{"--fork"}</code></nobr>     |       | Criar um fork da sessão ao continuar (use com `--continue` ou `--session`)      |
| <nobr><code>{"--share"}</code></nobr>    |       | Compartilhe a sessão                                                            |
| <nobr><code>{"--model"}</code></nobr>    | `-m`  | Modelo a ser usado na forma de provider/model                                   |
| <nobr><code>{"--agent"}</code></nobr>    |       | Agente a ser usado                                                              |
| <nobr><code>{"--file"}</code></nobr>     | `-f`  | Arquivo(s) a serem anexados à mensagem                                          |
| <nobr><code>{"--format"}</code></nobr>   |       | Formato: padrão (formatado) ou json (eventos JSON brutos)                       |
| <nobr><code>{"--title"}</code></nobr>    |       | Título para a sessão (usa o prompt truncado se nenhum valor for fornecido)      |
| <nobr><code>{"--attach"}</code></nobr>   |       | Anexe a um servidor hackbot em execução (por exemplo, http://localhost:4096)    |
| <nobr><code>{"--password"}</code></nobr> | `-p`  | Senha de autenticação básica (padrão: `HACKBOT_SERVER_PASSWORD`)                |
| <nobr><code>{"--username"}</code></nobr> | `-u`  | Usuário de autenticação básica (padrão: `HACKBOT_SERVER_USERNAME` ou `hackbot`) |
| <nobr><code>{"--dir"}</code></nobr>      |       | Diretório de execução, ou caminho no servidor remoto ao anexar                  |
| <nobr><code>{"--variant"}</code></nobr>  |       | Variante do modelo (nível de raciocínio específico do provedor)                 |
| <nobr><code>{"--thinking"}</code></nobr> |       | Mostrar blocos de pensamento                                                    |
| <nobr><code>{"--port"}</code></nobr>     |       | Porta para o servidor local (padrão para porta aleatória)                       |

---

### serve

Inicie um servidor hackbot sem cabeça para acesso à API. Confira a [documentação do servidor](/docs/server) para a interface HTTP completa.

```bash
hackbot serve
```

Isso inicia um servidor HTTP que fornece acesso à funcionalidade do hackbot sem a interface TUI. Defina `HACKBOT_SERVER_PASSWORD` para habilitar a autenticação básica HTTP (o nome de usuário padrão é `hackbot`).

#### Opções

| Flag                                     | Descrição                                             |
| ---------------------------------------- | ----------------------------------------------------- |
| <nobr><code>{"--port"}</code></nobr>     | Porta para escutar                                    |
| <nobr><code>{"--hostname"}</code></nobr> | Nome do host para escutar                             |
| <nobr><code>{"--mdns"}</code></nobr>     | Habilitar descoberta mDNS                             |
| <nobr><code>{"--cors"}</code></nobr>     | Origem(ns) de navegador adicionais para permitir CORS |

---

### session

Gerencie sessões do hackbot.

```bash
hackbot session [command]
```

---

#### list

Liste todas as sessões do hackbot.

```bash
hackbot session list
```

##### Opções

| Flag                                      | Curto | Descrição                                 |
| ----------------------------------------- | ----- | ----------------------------------------- |
| <nobr><code>{"--max-count"}</code></nobr> | `-n`  | Limitar às N sessões mais recentes        |
| <nobr><code>{"--format"}</code></nobr>    |       | Formato de saída: tabela ou json (tabela) |

---

### stats

Mostre o uso de tokens e estatísticas de custo para suas sessões do hackbot.

```bash
hackbot stats
```

#### Opções

| Flag                                    | Descrição                                                                                            |
| --------------------------------------- | ---------------------------------------------------------------------------------------------------- |
| <nobr><code>{"--days"}</code></nobr>    | Mostre estatísticas dos últimos N dias (todo o tempo)                                                |
| <nobr><code>{"--tools"}</code></nobr>   | Número de ferramentas a serem mostradas (todas)                                                      |
| <nobr><code>{"--models"}</code></nobr>  | Mostre a divisão do uso de modelos (oculto por padrão). Passe um número para mostrar os N principais |
| <nobr><code>{"--project"}</code></nobr> | Filtrar por projeto (todos os projetos, string vazia: projeto atual)                                 |

---

### export

Exporte dados da sessão como JSON.

```bash
hackbot export [sessionID]
```

Se você não fornecer um ID de sessão, será solicitado que você selecione entre as sessões disponíveis.

---

### import

Importe dados da sessão de um arquivo JSON ou URL de compartilhamento do hackbot.

```bash
hackbot import <file>
```

Você pode importar de um arquivo local ou de uma URL de compartilhamento do hackbot.

```bash
hackbot import session.json
hackbot import https://opncd.ai/s/abc123
```

---

### web

Inicie um servidor hackbot sem cabeça com uma interface web.

```bash
hackbot web
```

Isso inicia um servidor HTTP e abre um navegador para acessar o hackbot através de uma interface web. Defina `HACKBOT_SERVER_PASSWORD` para habilitar a autenticação básica HTTP (o nome de usuário padrão é `hackbot`).

#### Opções

| Flag                                     | Descrição                                             |
| ---------------------------------------- | ----------------------------------------------------- |
| <nobr><code>{"--port"}</code></nobr>     | Porta para escutar                                    |
| <nobr><code>{"--hostname"}</code></nobr> | Nome do host para escutar                             |
| <nobr><code>{"--mdns"}</code></nobr>     | Habilitar descoberta mDNS                             |
| <nobr><code>{"--cors"}</code></nobr>     | Origem(ns) de navegador adicionais para permitir CORS |

---

### acp

Inicie um servidor ACP (Agent Client Protocol).

```bash
hackbot acp
```

Este comando inicia um servidor ACP que se comunica via stdin/stdout usando nd-JSON.

#### Opções

| Flag                                     | Descrição                 |
| ---------------------------------------- | ------------------------- |
| <nobr><code>{"--cwd"}</code></nobr>      | Diretório de trabalho     |
| <nobr><code>{"--port"}</code></nobr>     | Porta para escutar        |
| <nobr><code>{"--hostname"}</code></nobr> | Nome do host para escutar |

---

### uninstall

Desinstale o hackbot e remova todos os arquivos relacionados.

```bash
hackbot uninstall
```

#### Opções

| Flag                                        | Curto | Descrição                                |
| ------------------------------------------- | ----- | ---------------------------------------- |
| <nobr><code>{"--keep-config"}</code></nobr> | `-c`  | Manter arquivos de configuração          |
| <nobr><code>{"--keep-data"}</code></nobr>   | `-d`  | Manter dados de sessão e snapshots       |
| <nobr><code>{"--dry-run"}</code></nobr>     |       | Mostrar o que seria removido sem remover |
| <nobr><code>{"--force"}</code></nobr>       | `-f`  | Pular prompts de confirmação             |

---

### upgrade

Atualiza o hackbot para a versão mais recente ou uma versão específica.

```bash
hackbot upgrade [target]
```

Para atualizar para a versão mais recente.

```bash
hackbot upgrade
```

Para atualizar para uma versão específica.

```bash
hackbot upgrade v0.1.48
```

#### Opções

| Flag                                   | Curto | Descrição                                                        |
| -------------------------------------- | ----- | ---------------------------------------------------------------- |
| <nobr><code>{"--method"}</code></nobr> | `-m`  | O método de instalação que foi usado; curl, npm, pnpm, bun, brew |

---

## Opções Globais

A CLI do hackbot aceita as seguintes flags globais.

| Flag                                       | Curto | Descrição                               |
| ------------------------------------------ | ----- | --------------------------------------- |
| <nobr><code>{"--help"}</code></nobr>       | `-h`  | Exibir ajuda                            |
| <nobr><code>{"--version"}</code></nobr>    | `-v`  | Imprimir número da versão               |
| <nobr><code>{"--print-logs"}</code></nobr> |       | Imprimir logs no stderr                 |
| <nobr><code>{"--log-level"}</code></nobr>  |       | Nível de log (DEBUG, INFO, WARN, ERROR) |

---

## Variáveis de ambiente

O hackbot pode ser configurado usando variáveis de ambiente.

| Variável                             | Tipo    | Descrição                                                            |
| ------------------------------------ | ------- | -------------------------------------------------------------------- |
| `HACKBOT_AUTO_SHARE`                 | boolean | Compartilhar sessões automaticamente                                 |
| `HACKBOT_GIT_BASH_PATH`              | string  | Caminho para o executável do Git Bash no Windows                     |
| `HACKBOT_CONFIG`                     | string  | Caminho para o arquivo de configuração                               |
| `HACKBOT_CONFIG_DIR`                 | string  | Caminho para o diretório de configuração                             |
| `HACKBOT_CONFIG_CONTENT`             | string  | Conteúdo de configuração json inline                                 |
| `HACKBOT_DISABLE_AUTOUPDATE`         | boolean | Desabilitar verificações de atualização automática                   |
| `HACKBOT_DISABLE_PRUNE`              | boolean | Desabilitar a poda de dados antigos                                  |
| `HACKBOT_DISABLE_TERMINAL_TITLE`     | boolean | Desabilitar atualizações automáticas do título do terminal           |
| `HACKBOT_PERMISSION`                 | string  | Configuração de permissões json inline                               |
| `HACKBOT_DISABLE_DEFAULT_PLUGINS`    | boolean | Desabilitar plugins padrão                                           |
| `HACKBOT_DISABLE_LSP_DOWNLOAD`       | boolean | Desabilitar downloads automáticos do servidor LSP                    |
| `HACKBOT_ENABLE_EXPERIMENTAL_MODELS` | boolean | Habilitar modelos experimentais                                      |
| `HACKBOT_DISABLE_AUTOCOMPACT`        | boolean | Desabilitar compactação automática de contexto                       |
| `HACKBOT_DISABLE_CLAUDE_CODE`        | boolean | Desabilitar leitura de `.claude` (prompt + habilidades)              |
| `HACKBOT_DISABLE_CLAUDE_CODE_PROMPT` | boolean | Desabilitar leitura de `~/.claude/CLAUDE.md`                         |
| `HACKBOT_DISABLE_CLAUDE_CODE_SKILLS` | boolean | Desabilitar carregamento de `.claude/skills`                         |
| `HACKBOT_DISABLE_MODELS_FETCH`       | boolean | Desabilitar busca de modelos de fontes remotas                       |
| `HACKBOT_FAKE_VCS`                   | string  | Provedor VCS falso para fins de teste                                |
| `HACKBOT_CLIENT`                     | string  | Identificador do cliente (padrão é `cli`)                            |
| `HACKBOT_ENABLE_EXA`                 | boolean | Habilitar ferramentas de busca web Exa                               |
| `HACKBOT_SERVER_PASSWORD`            | string  | Habilitar autenticação básica para `serve`/`web`                     |
| `HACKBOT_SERVER_USERNAME`            | string  | Substituir nome de usuário de autenticação básica (padrão `hackbot`) |
| `HACKBOT_MODELS_URL`                 | string  | URL personalizada para buscar configuração de modelos                |

---

### Experimental

Essas variáveis de ambiente habilitam recursos experimentais que podem mudar ou ser removidos.

| Variável                                       | Tipo    | Descrição                                                 |
| ---------------------------------------------- | ------- | --------------------------------------------------------- |
| `HACKBOT_EXPERIMENTAL`                         | boolean | Habilitar recursos experimentais sob o sinalizador global |
| `HACKBOT_EXPERIMENTAL_ICON_DISCOVERY`          | boolean | Habilitar descoberta de ícones                            |
| `HACKBOT_EXPERIMENTAL_DISABLE_COPY_ON_SELECT`  | boolean | Desabilitar cópia ao selecionar no TUI                    |
| `HACKBOT_EXPERIMENTAL_BASH_DEFAULT_TIMEOUT_MS` | number  | Tempo limite padrão para comandos bash em ms              |
| `HACKBOT_EXPERIMENTAL_OUTPUT_TOKEN_MAX`        | number  | Máximo de tokens de saída para respostas LLM              |
| `HACKBOT_EXPERIMENTAL_FILEWATCHER`             | boolean | Habilitar monitoramento de arquivos para todo o diretório |
| `HACKBOT_EXPERIMENTAL_OXFMT`                   | boolean | Habilitar formatador oxfmt                                |
| `HACKBOT_EXPERIMENTAL_LSP_TOOL`                | boolean | Habilitar ferramenta LSP experimental                     |
| `HACKBOT_EXPERIMENTAL_DISABLE_FILEWATCHER`     | boolean | Desabilitar monitoramento de arquivos                     |
| `HACKBOT_EXPERIMENTAL_EXA`                     | boolean | Habilitar recursos experimentais do Exa                   |
| `HACKBOT_EXPERIMENTAL_LSP_TY`                  | boolean | Habilitar TY LSP para arquivos python                     |
| `HACKBOT_EXPERIMENTAL_PLAN_MODE`               | boolean | Habilitar modo de plano                                   |
| `HACKBOT_EXPERIMENTAL_BACKGROUND_SUBAGENTS`    | boolean | Habilitar tarefas de subagentes em segundo plano          |
| `HACKBOT_EXPERIMENTAL_EVENT_SYSTEM`            | boolean | Habilitar sistema de eventos experimental                 |
| `HACKBOT_EXPERIMENTAL_NATIVE_LLM`              | boolean | Habilitar caminho nativo de requisição LLM                |
| `HACKBOT_EXPERIMENTAL_PARALLEL`                | boolean | Habilitar execução paralela de busca web                  |
| `HACKBOT_EXPERIMENTAL_SCOUT`                   | boolean | Habilitar subagente Scout                                 |
| `HACKBOT_EXPERIMENTAL_WORKSPACES`              | boolean | Habilitar suporte a espaços de trabalho                   |
